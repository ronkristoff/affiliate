import UserTimelineClient from "./UserTimelineClient";

export const metadata = {
  title: "User Timeline | saligadmin",
};

export default function UserTimelinePage() {
  return <UserTimelineClient />;
}
